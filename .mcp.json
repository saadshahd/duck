{
  "mcpServers": {
    "fallow": {
      "command": "node_modules/.bin/fallow-mcp"
    },
    "json-render-editor": {
      "command": "bun",
      "args": [
        "run",
        "packages/mcp-server/src/index.ts"
      ]
    },
    "expect": {
      "command": "npx",
      "args": [
        "-y",
        "expect-cli@latest",
        "mcp"
      ]
    }
  }
}
