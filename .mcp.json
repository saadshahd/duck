{
  "mcpServers": {
    "fallow": {
      "command": "node_modules/.bin/fallow-mcp"
    },
    "duck": {
      "command": "bun",
      "args": [
        "run",
        "packages/mcp-server/src/index.ts"
      ]
    },
    "expect": {
      "command": "npx",
      "args": [
        "-y",
        "expect-cli@latest",
        "mcp"
      ]
    }
  }
}
