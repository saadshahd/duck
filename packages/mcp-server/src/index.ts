/**
 * MCP Server for json-render-editor
 *
 * Stub — will be implemented in Phase 4.
 * When complete, exposes tools for AI agents to:
 *   - Read/edit json-render documents
 *   - Query component catalogs
 *   - Capture preview screenshots
 *   - Manage checkpoints/history
 */
console.log("json-render-editor MCP server — not yet implemented");
console.log(
  "Run `bun run dev` in the editor package to use the visual editor.",
);
