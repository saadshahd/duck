{
  "name": "json-render-editor",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --cwd packages/editor dev",
    "build": "bun run --cwd packages/editor build",
    "typecheck": "tsc --noEmit -p packages/editor && tsc --noEmit -p packages/mcp-server"
  },
  "dependencies": {
    "@tanstack/react-form": "^1.28.5",
    "timeago.js": "^4.0.2",
    "tinykeys": "^3.0.0"
  }
}
