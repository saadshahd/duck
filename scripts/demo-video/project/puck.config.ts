// Demo-video MCP project catalog: re-export the demo editor's catalog so the
// mcp-server drives the exact config the browser at :5173 renders with.
export { config } from "../../../packages/editor/src/demo/puck.config.js";
